specVersion: "v1alpha"
serviceId: "dataconnect"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "postgres"
      cloudSql:
        instanceId: "my-cloud-sql"
connectorDirs: ["./connector"]
